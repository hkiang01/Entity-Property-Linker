/**
 * An Entity corresponding to the DB's `entity` table.
 * This schema is available to be imported
 */
export class Entity {
  constructor(id, name) {
    this.id = id;
    this.name = name;
  }
}
